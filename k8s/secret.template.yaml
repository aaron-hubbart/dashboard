# DO NOT commit real values. Use the kubectl command in the runbook.
# This file is a reference template only.
apiVersion: v1
kind: Secret
metadata:
  name: dashboard-secrets
  namespace: tam-dashboard
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "REPLACE"
  AZURE_CLIENT_ID: "REPLACE"
  AZURE_CLIENT_SECRET: "REPLACE"
  AZURE_TENANT_ID: "REPLACE"
  SESSION_SECRET: "REPLACE-32-chars"
  # Zoom Server-to-Server OAuth — create a S2S app at marketplace.zoom.us
  # Required scopes: cloud_recording:read:list_user_recordings, cloud_recording:read:content,
  #                   meeting:read:assets, meeting:read:search
  ZOOM_ACCOUNT_ID: "REPLACE"
  ZOOM_CLIENT_ID: "REPLACE"
  ZOOM_CLIENT_SECRET: "REPLACE"
  ZOOM_TOKEN: ""  # optional static fallback; leave empty if using S2S above
  ZOOM_WEBHOOK_SECRET: "REPLACE"
